**🔍 LLM Hallucination Detector**

"ChatGPT said it. But is it actually true?"
A real-time hallucination detection system that sits inside your browser, automatically checks AI responses against retrieved evidence, and tells you whether to trust the answer — before you act on it.

**🚨 The Problem**

Every day, millions of people trust AI answers that are completely wrong.

A US lawyer submitted fake court cases generated by ChatGPT — and got fined by a judge
Students submit invented research citations that don't exist
Professionals make decisions based on fabricated statistics

There is no warning. No flag. No way to know. Until now.

**✅ What This Does**

You ask ChatGPT a question
         ↓
ChatGPT gives an answer
         ↓
Our system fetches real evidence from trusted sources
         ↓
Fine-tuned DeBERTa-v3 checks consistency
         ↓
Chrome Extension shows ✅ Factual or ⚠️ Hallucination
— right inside the ChatGPT page
No copy-pasting. No separate tools. Just an instant badge on every response.

**🧠 How It Works**

**1. RAG Pipeline**

When an AI response is detected, the system embeds the question using sentence-transformers and searches a FAISS vector index for the most relevant real-world evidence.

**2. DeBERTa-v3 Classifier**

A fine-tuned DeBERTa-v3-base model (184M parameters) takes the AI answer and retrieved evidence as input and outputs:
✅ FACTUAL — answer is consistent with evidence
⚠️ HALLUCINATION — answer contradicts real sources
❓ UNCERTAIN — not enough evidence to judge
**3. Chrome Extension**

Results are injected directly into the ChatGPT/Gemini interface as a badge — no extra steps needed.

**📊 Model Performance**

| Accuracy    | 87.05% |

| F1 Weighted | 86.90% |

| F1 Macro    | 86.27% |

**🛠️ Tech Stack**
| Layer | Technology |
|-------|------------|
| NLP Model | DeBERTa-v3-base (Microsoft) |
| Training Data | FEVER NLI + TruthfulQA |
| Vector Search | FAISS + sentence-transformers |
| Backend | FastAPI + Docker |
| Frontend | Chrome Extension (Manifest v3) |
| Deployment | Render / Railway |

**📁 Project Structure**

llm-hallucination-detector/

├── data/ 

├── notebooks/ 

│   ├── 01_load_data.ipynb

│   ├── 02_EDA.ipynb

│   ├── 03_RAG_pipeline.ipynb

│   ├── 04_preprocessing.ipynb

│   └── 05_train_deberta.ipynb

├── backend/ 

├── extension/

└── models/

**🚨 Why This Matters**

Existing tools like Galileo and Braintrust cost thousands of dollars and target enterprise developers. This is the first free, open-source, consumer-facing hallucination detector that works directly inside your browser — for students, journalists, lawyers, and anyone who uses AI daily

**👩‍💻 Built By**

Nivetha G — M.Sc. AI & ML, Coimbatore Institute of Technology

GitHub: https://github.com/Nivetha15122006/hallucination-detector
Model: https://huggingface.co/NiviG/hallucination-detector

---

## 🚀 How to Setup and Run

Follow these steps to run the hallucination detector locally on your machine:

### 1. Installation & Environment Setup
Clone the repository and set up a Python virtual environment:
```bash
git clone https://github.com/Nivetha15122006/hallucination-detector.git
cd hallucination-detector

# Create and activate virtual environment
python -m venv venv
# On Windows:
.\venv\Scripts\activate
# On Linux/macOS:
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt
```

### 2. Build the Knowledge Base (RAG Index)
Run the automated downloader to fetch Wikipedia pages and build the FAISS vector index:
```bash
python backend/build_knowledge_base.py
```
*This will generate the indexed vectors inside the `data/` directory.*

### 3. Run and Test Locally (No Server Required)
You can evaluate the retriever and the DeBERTa model accuracy directly in your terminal:
```bash
python backend/test_detector.py
```

### 4. Run the FastAPI Backend Server
Start the web API using Uvicorn:
```bash
python backend/main.py
```
*The server will start listening at `http://localhost:8000`.*

### 5. Install the Chrome Extension
1. Open Google Chrome and navigate to `chrome://extensions/`.
2. Toggle on **Developer mode** in the top-right corner.
3. Click **Load unpacked** in the top-left corner.
4. Select the **`extension/`** folder inside your project directory.
5. Go to ChatGPT, ask a question (e.g. *"Who invented the telephone?"*), and see the factual verification badges render automatically!
